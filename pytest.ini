[pytest]
asyncio_mode = auto
strict_markers = true
markers =
    real: requires explicit LOFTER_RUN_LIVE=1 and accesses live LOFTER services
    live_hybrid: fetches one live anonymous canary before enforcing offline production flow
    adapter_contract: uses real AstrBot/aiocqhttp with an in-process NapCat-compatible reverse WebSocket fake
