{
    "lofter_cookie": {
        "description": "Lofter 登录 Cookie（可选，遗留配置）。抓取已改用 app 接口，公开内容无需登录即可获取；留空即可",
        "type": "string",
        "default": ""
    },
    "poll_interval": {
        "description": "订阅轮询间隔（分钟）",
        "type": "int",
        "default": 30
    },
    "max_images": {
        "description": "解析帖子时最多展示的图片数量",
        "type": "int",
        "default": 3
    },
    "search_limit": {
        "description": "搜索结果最多返回的条数（最大 100，超过 20 条自动翻页）",
        "type": "int",
        "default": 3
    },
    "text_file_threshold": {
        "description": "文字贴超过这个字数时，整理成 .txt 文件发送而不是合并转发；文件名取文章标题",
        "type": "int",
        "default": 1000
    },
    "video_max_mb": {
        "description": "解析视频贴时允许下载的最大体积（MB），超过则只发文字不发文件",
        "type": "int",
        "default": 100
    },
    "temp_file_ttl_hours": {
        "description": "生成的全文 .txt 和视频文件保留多久（小时），到期后由后台定期清理；最小 1 小时",
        "type": "int",
        "default": 1
    }
}
