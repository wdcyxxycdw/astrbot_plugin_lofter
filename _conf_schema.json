{
    "lofter_cookie": {
        "description": "Lofter 登录 Cookie（可选，遗留配置）。抓取已改用 app 接口，公开内容无需登录即可获取；留空即可",
        "type": "string",
        "default": ""
    },
    "poll_interval": {
        "description": "订阅轮询间隔（分钟）",
        "type": "int",
        "default": 30
    },
    "max_images": {
        "description": "解析帖子时最多展示的图片数量",
        "type": "int",
        "default": 3
    },
    "search_limit": {
        "description": "搜索结果最多返回的条数（最大 100，超过 20 条自动翻页）",
        "type": "int",
        "default": 3
    },
    "parse_reaction": {
        "description": "自动解析链接时，给触发的消息贴表情表示进度（仅 QQ/OneBot 平台生效）",
        "type": "bool",
        "default": true
    },
    "parse_reaction_emoji": {
        "description": "「正在解析」的表情 ID，默认 128064（👀）。QQ 只接受白名单内的 ID，贴不上时日志会记录 NapCat 的报错，换一个即可",
        "type": "int",
        "default": 128064
    },
    "parse_reaction_done_emoji": {
        "description": "「解析完成」的表情 ID，默认 124（OK）",
        "type": "int",
        "default": 124
    },
    "parse_reaction_failed_emoji": {
        "description": "「解析失败」的表情 ID，默认 123（NO）",
        "type": "int",
        "default": 123
    }
}
